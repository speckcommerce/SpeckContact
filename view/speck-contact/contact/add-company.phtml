<?php

$form = $this->form;
$form->prepare();
if ($contactId) {
    $form->setAttribute('action', $this->url('zfcadmin/contact/contact/add-company', array('id' => $contactId)));
} else {
    $form->setAttribute('action', $this->url('zfcadmin/contact/company/add-company'));
}
$form->setAttribute('method', 'post');
$form->setAttribute('class', 'form label-inline');

?>
<h2>Add Company</h2>
<div class="well">
    <?=$this->form()->openTag($form);?>
    <?php foreach ($form->getElements() as $element) : ?>
        <?php if ($element->getLabel() != null) : ?>
            <?=$this->formLabel($element);?>
        <?php endif; ?>
        <?=$this->formElement($element);?>
        <?=$this->formElementErrors($element);?>
    <?php endforeach; ?>
    <br />
    <input type="submit" value="Save" class="btn" />
    <?=$this->form()->closeTag()?>
</div>
